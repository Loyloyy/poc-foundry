Implement `slugify(text)` in `slug.py` (currently unimplemented — the test is RED):
  - lowercase the text;
  - replace each run of non-alphanumeric characters with a single hyphen `-`;
  - strip leading/trailing hyphens.
e.g. "Hello World!" -> "hello-world", "  A  B " -> "a-b", "C++ & Rust" -> "c-rust".
Do not edit the test.
