`parse_config(text)` in `cfg.py` parses a JSON string into the equivalent Python object.
It currently calls a non-existent API — fix it to use the correct standard-library call.
Do not edit the test.
