`median(xs)` in `stats.py` returns the median of a non-empty list of numbers (the average of the
two middle values when the length is even). It currently returns the wrong value — fix the logic.
Do not edit the test.
